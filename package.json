{
  "name": "bisect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An agent that investigates bug reports using real user sessions, not guesses.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "once": "tsx src/index.ts --once",
    "typecheck": "tsc --noEmit",
    "ui": "tsx src/ui/server.ts",
    "bench": "tsx benchmark/run.ts",
    "review": "tsx src/pr-review/server.ts",
    "review:pr": "tsx src/pr-review/server.ts --pr",
    "setup:github-app": "tsx src/pr-review/setupGithubApp.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "openai": "^6.9.1",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.1",
    "typescript": "^5.9.3"
  }
}
